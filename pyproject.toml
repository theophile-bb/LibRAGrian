[project]
name = "libragrian"
version = "0.1.0"
description = "A small library for RAG utilities"
readme = "README.md"
requires-python = ">=3.12"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
